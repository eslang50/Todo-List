import { display, displayProjects, addProject, submitProjectForm, displayInbox, displayToday, displayWeek, displayProject, removeProject, addTask } from "./display";

window.addEventListener('DOMContentLoaded', displayProjects(), addProject(), displayProject(), removeProject(), addTask());







